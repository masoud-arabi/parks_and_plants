 <div>
 <%#= image_tag @garden.banner_url, alt:"name" %>
<h1>garden :<%= @garden.name %> </h1>
</div>


<%= simple_form_for([@garden, @plant]) do |f| %>
        <%= f.input :name %>
        <%= f.input :image_url %>
        <%= f.submit 'Save your plant', class: 'btn btn-primary' %>
      <% end %>